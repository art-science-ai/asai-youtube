### CLI
`soffice` is the CLI executable

```bash
# Convert to pdf or other
soffice --headless --convert-to pdf filename.docx
```


### Installation
- For linux, use nixpkgs
- For macos, use homebrew; nixpkgs version doesn't build for macos