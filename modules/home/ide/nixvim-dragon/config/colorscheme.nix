# Catppuccin colorscheme with system theme sync
{
  colorschemes.catppuccin = {
    enable = true;
    settings = {
      # Default to macchiato (dark), can be overridden
      flavour = "macchiato";
      transparent_background = false;
      integrations = {
        # Will add integrations as we add plugins
      };
    };
  };
}