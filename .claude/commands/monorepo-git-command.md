---
description: Git operations for the monorepo (commit, push, status)
---

# Monorepo Git Command

- Use the monorepo-git skill to handle git operations in this repository. CRITICAL: you MUST invoke the monorepo-git skill.
- If the user has provided any additional context, pass that into the skill invocation. Here is the context provided by the user: $ARGUMENTS
